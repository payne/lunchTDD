<p id="notice"><%= notice %></p>

<h1>Listing Groups</h1>

<!-- TODO: do this with bootstrap! -->
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>
        <td><%= group.title %></td>
        <td><%= group.description %></td>
        <td><%= link_to 'Show', group %></td>
<%# <td><%= link_to 'Edit', edit_group_path(group) pct></td> %>
       <%# <td><%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } pct></td> %>
      </tr>
    <% end %>
  </tbody>
</table>
